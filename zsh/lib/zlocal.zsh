# Add any local environment variables or configurations specific
# zlocal.zsh file. It will be sourced by your main zsh configuration but
# is not tracked by git via `git update-index --skip-worktree <file_name>`
